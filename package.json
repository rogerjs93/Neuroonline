{
  "name": "neuroonline",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/Neuroonline/",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist" // <-- Add this line
  },
  "devDependencies": {
    "gh-pages": "^6.1.1", // Version might differ slightly
    "vite": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
